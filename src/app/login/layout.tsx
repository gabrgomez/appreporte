export const metadata = {
    title: 'Iniciar sesión - Global Holding',
  };
  
  export default function LoginLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
  }
  